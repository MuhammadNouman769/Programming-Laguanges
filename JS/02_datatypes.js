"use strict"; // treat all JS code as newer version

// alert(3 + 3) // we are using node js not browser

// code should be readable
// code readability is exactly what we want:
// like this:
// console.log(3 
//     + 3); // not like this:
// like this:
// console.log(3 + 3);

/// data types in js:
// number => 2 to power 53 like 2**53
// bigint => for larger numbers like 2**53 + 1
// string => "hello" or 'hello' or `hello`
// boolean => true/false (only two values)
// null => standalone value (it is an object)  
// undefined => value is not assigned (it is an object)
// symbol => unique value (it is an object)
// object => {} (it is an object)    

let name = "Usama";
let age = 22;
let isLoggedIn = false;

console.log(typeof null); // object interview question) what is null in js answer is object but it is a bug in js
console.log(typeof undefined); // undefined  

