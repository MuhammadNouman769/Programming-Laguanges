# Part 11: Strings

This part practices indexing, slicing, case conversion, searching, and formatting text.

## Practice files

- `task1.py`: reverse a string.
- `task2.py`: count vowels.
- `task3.py`: normalize a title.
- `task4.py`: extract a slice.
- `mini_project.py`: format a contact card.

`strings.py` is the original slicing example.

## Original lesson content

### `strings.py`
The original one-line lesson demonstrates string slicing and leaves the resulting slice available for practice.

```python
text = "Python"
print(text[1:4])
```