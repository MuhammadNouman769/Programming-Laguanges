# Part 12: Data types

This part reviews numeric, text, sequence, set, mapping, and boolean values.

## Practice files

- `task1.py`: inspect a value type.
- `task2.py`: count values in a list.
- `task3.py`: remove duplicates with a set.
- `task4.py`: read a dictionary field.
- `mini_project.py`: build a typed inventory.

`data_types.py` is the original type example.

## Original lesson content

### `data_types.py`
Creates common built-in values and prints their values and types.

```python
number = 10
decimal = 2.5
text = "Python"
items = [number, decimal, text]
print(items, type(items))
```