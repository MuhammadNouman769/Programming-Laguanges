# Part 8: Loops

This part practices repeated work with `for`, `while`, ranges, counters, and break conditions.

## Practice files

- `task1.py`: create a multiplication table.
- `task2.py`: count even values.
- `task3.py`: calculate a factorial with a loop.
- `task4.py`: reverse text with a loop.
- `mini_project.py`: run a menu-free number analyzer.

The existing `loop.py` is the original loop example.

## Original lesson content

### `loop.py`
Practices `for` and `while` loops with ranges, sums, factorials, prime checks, string reversal, and counting letters, digits, and special characters.

```python
total = 0
for number in range(1, 6):
	total += number
print(total)
```